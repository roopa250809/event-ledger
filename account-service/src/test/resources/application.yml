spring:
  application:
    name: account-service
  datasource:
    url: jdbc:h2:mem:account-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
management:
  tracing:
    sampling:
      probability: 1.0
event-ledger:
  security:
    service-api-key: test-service-key-1234567890
