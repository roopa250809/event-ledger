spring:
  datasource:
    url: jdbc:h2:mem:account-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
management:
  tracing:
    sampling:
      probability: 1.0
